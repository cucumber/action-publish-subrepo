name: Unit tests

on:
  push:

jobs:
  run-unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get update && apt-get install git-filter-repo
      - run: ./test

