name: Trigger tests

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: aurelien-baudet/workflow-dispatch@v2
        with:
          workflow: Publish subrepo
          token: ${{ secrets.TOKEN }}